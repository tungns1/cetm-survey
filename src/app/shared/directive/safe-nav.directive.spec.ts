import { SafeNavDirective } from './safe-nav.directive';

describe('SafeNavDirective', () => {
  it('should create an instance', () => {
    const directive = new SafeNavDirective();
    expect(directive).toBeTruthy();
  });
});
